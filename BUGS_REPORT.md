# 📋 Отчет о дефектах и состоянии проекта

**Ветка:** `6rm2jx-codex/ensure-section-connectivity-for-employees` (актуальная)
**Дата:** 24.01.2025
**Статус:** Приложение работает, критические баги исправлены.

---

## 🛠 Технические особенности среды
⚠️ **ВНИМАНИЕ:** В данной среде разработки **PowerShell работает нестабильно** (падает с ошибками парсера).
- Не запускайте сложные команды через `run_terminal_cmd` в PowerShell.
- Используйте **CMD** или создавайте `.bat` файлы для выполнения команд (например, `npm install`, `npm run dev`).
- Сервер запускается на порту **5175** (настроен в `vite.config.ts`), но иногда Vite может занимать случайный порт (5173), если 5175 занят зависшим процессом.
- Для перезапуска используйте скрипт `kill_and_restart.bat`.

---

## 🐛 Найденные дефекты

### 1. [Backend] Отсутствует соединение с сервером (500 Error)
**Описание:** При загрузке приложения возникают ошибки 500 при запросе к `/api/graphs`.
**Причина:** Локальный бэкенд-сервер (порт 4000) не запущен или недоступен.
**Влияние:** Приложение работает на мок-данных (fallback), создание и сохранение графов невозможно.
**Логи:**
```
Failed to load resource: the server responded with a status of 500 (Internal Server Error) @ /api/graphs
```

### 2. [Frontend] Ошибка пропсов в компоненте Steps (React Warning)
**Описание:** В консоли появляется предупреждение React.
**Причина:** В компонент `Steps` (из `@consta/uikit`) или его родитель передается проп `%s`, который попадает в DOM.
**Локация:** `src/components/InitiativePlanner.tsx` (компонент `InitiativePlanner`).
**Логи:**
```
Warning: React does not recognize the `%s` prop on a DOM element... spell it as lowercase `%s` instead...
```

### 3. [Frontend] Отсутствует favicon (404)
**Описание:** Браузер не может загрузить иконку сайта.
**Логи:** `Failed to load resource: the server responded with a status of 404 (Not Found) @ /favicon.ico`

---

## ✅ Исправленные проблемы

### 1. Критический баг фильтрации
В файле `src/App.tsx` была исправлена логика фильтрации по продуктам.
**Было:** Если фильтр пустой -> возвращалось `false` (скрыть всё).
**Стало:** Если фильтр пустой -> возвращается `true` (показать всё).

---

## 📝 Рекомендации для следующего разработчика

1. **Запуск проекта:**
   Используйте `kill_and_restart.bat` для чистого запуска сервера на порту 5175.

2. **Бэкенд:**
   Для полноценной работы нужно запустить серверную часть (папка `server/`).

3. **UI Kit:**
   Обратите внимание на предупреждение в `InitiativePlanner.tsx` — возможно, опечатка в использовании компонента Consta UI.



